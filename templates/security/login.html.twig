{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}
{#% form_theme loginForm 'bootstrap_4_layout.html.twig' %#}

{% block body %}
<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
        <div class="mb-3">
            <p>Vous êtes deja enregistrer en tant que : {{ app.user.username }}, <a href="{{ path('app_logout') }}">Se deconnecter</a></p>
        </div>
    {% endif %}
    <div class="container">
        <div class="row">
            <div class="d-flex flex-column justify-content-center align-items-center">
                <div class="col-9">
                    <h3 class="text-center mb-3">Merci de vous enregistrer</h3>
                    <label for="inputEmail">E-mail</label>
                    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
                    <label for="inputPassword">Mot de passe</label>
                    <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>

                    <input type="hidden" name="_csrf_token"
                        value="{{ csrf_token('authenticate') }}"
                    >

                    
                    {#
                        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                        See https://symfony.com/doc/current/security/remember_me.html

                        <div class="checkbox mb-3">
                            <label>
                                <input type="checkbox" name="_remember_me"> Remember me
                            </label>
                        </div>
                    #}
                </div>
            </div>
            <div class="d-flex justify-content-around">
                <div class="col-3">
                    <button class="btn btn-lg btn-primary mt-4" type="submit">Valider</button>
                </div>
                <div class="col-3">
                    <a class="btn btn-lg btn-primary mt-4" href="{{path('app_register')}}">Pas encore de compte ?</a>
                </div>
            </div>
        </div>
    </div>
</form>

{% endblock %}
